import React from "react";
import { assert, expect } from "chai";
import { configure, mount, shallow } from "enzyme";
import Adapter from "enzyme-adapter-react-16";
import App from "../App";
import ReportList from "../ReportList/ReportList";
import ScheduleList from "../ScheduleList/ScheduleList";

configure({ adapter: new Adapter() });

describe("App", () => {
	let component = null;

	describe("App generado", () => {
        let config = {
            apiUrl: "",
            lang: "es",
            langOptions: [],
            categoryUrl: ""
        };

		it("renders App must be 1", () => {
			component = mount(<App {...config} />);
			assert.strictEqual(1, component.find(App).length);
        });

        it("Show List", () => {
			component = mount(<App {...config} />);
			assert.strictEqual(1, component.find(ReportList).length);
        });

        // it("Show Schedule", () => {
        //     const wrapper = shallow(<App {...config} />);
		// 	wrapper.instance().showScheduleList({
        //         id: 2,
        //         appCode: "",
        //         name: "",
        //         type: "",
        //         description: "",
        //         createdTimestamp: "",
        //         formats: ["PDF"],
        //     });
		// 	wrapper.update();
		// 	assert.strictEqual(1, wrapper.find(ScheduleList).length);
        // });
     
    });
    
});
